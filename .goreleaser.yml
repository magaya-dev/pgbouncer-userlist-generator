builds:
  - main: ./cmd
    id: "pgbouncer-userlist-generator"
    binary: pgbouncer-userlist-generator
    goos:
      - linux
changelog:
  sort: asc
  filters:
    exclude:
    - '^docs:'
    - '^test:'
